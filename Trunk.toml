# Configuracao do build web (Trunk).
# dist = docs -> o GitHub Pages serve a pasta /docs da branch main.
# public_url -> prefixo do caminho no Pages de projeto (f0g2.github.io/Ramsey_Putnam/).
[build]
dist = "docs"
public_url = "/Ramsey_Putnam/"
